enum MuscleGroup { chest, back, legs, arms, shoulders, core, fullBody }

enum Difficulty { beginner, intermediate, advanced }

enum Equipment { dumbbell, barbell, machine, bodyweight, cable }

enum WorkoutStatus { scheduled, completed, skipped }

enum GoalStatus { active, completed, abandoned }

enum RoutineGoal { hypertrophy, strength, endurance, powerlifting }
